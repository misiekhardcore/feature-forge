{
  "$schema": "https://raw.githubusercontent.com/misiekhardcore/feature-forge/main/packages/cli/src/flows/flow-schema.json",
  "name": "review",
  "command": "/review",
  "routines": {
    "inspect": {
      "params": [
        {
          "name": "output",
          "description": "The raw output from the previous step",
          "required": true
        },
        {
          "name": "workspace",
          "description": "Absolute path to the workspace where the code lives"
        }
      ],
      "steps": [
        {
          "type": "agent",
          "id": "review",
          "systemPrompt": "review",
          "workingDir": {
            "path": "{{workspace}}"
          },
          "parseJson": true,
          "prompt": "Review the code for quality: correctness, architecture, standards compliance, error handling, type safety, SOLID principles, and test quality.\n\nTask:\n{{prompt}}\n\nPrevious step output:\n{{output}}"
        }
      ]
    }
  }
}
