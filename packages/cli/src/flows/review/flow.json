{
  "$schema": "https://raw.githubusercontent.com/misiekhardcore/feature-forge/main/packages/cli/src/flows/flow-schema.json",
  "name": "review",
  "command": "/review",
  "routines": {
    "inspect": {
      "description": "Review code changes in a workspace against a task description",
      "params": [
        {
          "name": "workspace",
          "description": "Absolute path to the git worktree containing the changes"
        },
        {
          "name": "task",
          "description": "The task description to review against"
        },
        {
          "name": "build_output",
          "description": "Output from the build step (agent output, test results, etc.)"
        }
      ],
      "steps": [
        {
          "type": "agent",
          "id": "review",
          "systemPrompt": "review",
          "workingDir": {
            "path": "{{workspace}}"
          },
          "parseJson": true,
          "prompt": "Review the code changes against:\n{{task}}\n\nBuild output:\n{{build_output}}"
        }
      ],
      "input_schema": {
        "workspace": "string",
        "task": "string",
        "build_output": "string"
      },
      "output_schema": {
        "passed": "boolean",
        "summary": "string"
      }
    }
  }
}
