{
  "name": "@feature-forge/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared abstractions for feature-forge",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'shared: nothing to build'",
    "test": "vitest run",
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.79.8"
  },
  "devDependencies": {
    "typebox": "1.3.0",
    "@feature-forge/eslint-config": "*"
  }
}
