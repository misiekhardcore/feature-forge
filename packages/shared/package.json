{
  "name": "@feature-forge/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared abstractions for feature-forge",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'shared: nothing to build'",
    "test": "vitest run",
    "lint": "echo 'shared: linted via root'",
    "format": "echo 'shared: formatted via root'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.79.8"
  },
  "devDependencies": {
    "typebox": "1.3.0"
  }
}
